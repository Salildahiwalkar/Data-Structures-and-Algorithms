#include<bits/stdc++.h>
using namespace std;

int main(){
   unordered_set<int> us; // no order only unique ness
   /* lower bound and upper bound function 
    does not works, rest all functions are same
    as above, ot does not store in any particular order it has a better complexity than set in
    most cases, except some when collision happens*/
}