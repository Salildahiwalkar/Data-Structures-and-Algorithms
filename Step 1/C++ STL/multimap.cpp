#include <bits/stdc++.h>
using namespace std;

int main(){
    // MultiMap - no unique only sorted i.e. duplicate allowed
    // everything same as map, only it can store multiple keys
    // only mpp[key] cannot be used here
    // mostly constant time complexity O(1) only in very worst case linear time complexity O(n)
}